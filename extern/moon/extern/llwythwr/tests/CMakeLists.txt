add_executable(tests test.cpp)
target_link_libraries(tests PRIVATE Catch2::Catch2WithMain llwythwr)
target_compile_features(tests PRIVATE cxx_std_17)
include(CTest)
include(Catch)
catch_discover_tests(tests)

add_subdirectory(sample_dll)
add_dependencies(tests sample_dll)
if(WIN32)
    add_subdirectory(aaa_dll)
    add_dependencies(tests aaa_dll)
    add_subdirectory(bbb_dll)
    add_dependencies(tests bbb_dll)
endif()